import ServerMiddleware from './index/features/ServerMiddleware.mdx'

From the server-side point of view, `vite-plugin-ssr` is just a server middleware. It's versatile and we can use it any server environment we want.

<ServerMiddleware />
