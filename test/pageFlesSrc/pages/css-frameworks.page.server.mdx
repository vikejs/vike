import { Integration } from '../components/Integration'

`vite-plugin-ssr` is being used with all kinds of CSS frameworks; we provide guides only when integration is not straightforward.

<Integration toolTypeName="CSS framework" />
