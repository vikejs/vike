import { Note, Link } from '@brillout/docpress'

<Note type="warning">`dist/server/importBuild.js` is outdated and not needed anymore.</Note>

> There are still some (rare) situations when we need to manually import the `dist/server/` files, see <Link href="/importBuild.cjs" />.
