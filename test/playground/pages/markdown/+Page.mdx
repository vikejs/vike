export const frontmatter = {
  title: 'Some title set in mdx'
}

import { Counter } from '../../pages/index/Counter';

# Side export .md file

TEST: being able to define a config as a "side export" in a `.md` file, such as frontmatter data. (See `export { frontmatter }` in `/pages/markdown/+Page.mdx`.)

This page is written in _Markdown_.

<Counter/>
