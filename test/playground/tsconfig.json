{
  "compilerOptions": {
    "strict": true,
    "jsx": "react",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "types": ["vite/client"],
    "esModuleInterop": true,
    "target": "ES2020",
    // ESNext (not ES2022) so that import attributes are supported: `with { type: 'vike:pointer' }`
    "module": "ESNext",
    "moduleResolution": "Bundler"
  },
  "include": [
    "**/*",
    // Include .test* files
    // https://github.com/microsoft/TypeScript/issues/49555
    "**/.*"
  ]
}
