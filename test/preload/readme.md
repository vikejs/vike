Test [`injectFiler()`](https://vike.dev/preloading#injectfilter).

This test:
 - Uses Vitest in order to be able to use `toMatchSnapshot()` (which `@brillout/test-e2e` doesn't support).
 - Is a standalone test to make snapshots more stable.
