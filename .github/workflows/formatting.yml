name: 'Check formatting'
on:
  push:
jobs:
  check_formatting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - run: pnpm install
      - run: FORCE_COLOR=1 pnpm run format:check
        env:
          FORCE_COLOR: "1"

