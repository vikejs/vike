{
  "workspaces": [
    "src/"
  ],
  "scripts": {
    "// Demo": "",
    "demo": "cd examples/vue/ && npm run start",
    "// Dev": "",
    "dev": "npm install && npm run build:watch",
    "// Build": "",
    "build": "npm install && npm run build:once",
    "build:once": "cd src/ && tsc --build",
    "build:watch": "cd src/ && tsc --build --watch"
  }
}
