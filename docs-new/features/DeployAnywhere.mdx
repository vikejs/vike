import ControlServer from './ControlServer.mdx'
import { PrerenderPreview } from './PrerenderPreview'

### Any Server Environment

<ControlServer />

### Pre-rendering

You can pre-render your pages to HTML and remove the need for a production server.

<PrerenderPreview />

You can then deploy to any static host (Netlify, GitHub Pages, Cloudflare Pages, ...).
