From an architectural point of view, `vite-plugin-ssr` is simply a server middleware we can use with any server environment we want (Cloudflare Workers, EC2 instance, AWS lambda, Firebase, Express.js, Fastify, Hapi, ...)
