import { JustAMiddlewareLink } from '../../components'
import { Link } from '@brillout/docpress'

<JustAMiddlewareLink /> This means you you can use any deployment strategy and any deployment provider.

You can also <Link text="pre-render" href="/pre-rendering" /> your pages to remove the need for a production server and deploy to a <Link text="static host" href="/static-hosts" /> instead.
