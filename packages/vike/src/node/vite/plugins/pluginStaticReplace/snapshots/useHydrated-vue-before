import { useHydrated } from 'vike-vue/useHydrated'

export function SomeComponent() {
  const hydrated = useHydrated()
  if (hydrated.value) {
    return 'Hydrated!'
  } else {
    return 'SSR'
  }
}
