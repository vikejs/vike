Example of using `vite-plugin-ssr` with React and Apollo.

To run it:

```bash
git clone git@github.com:brillout/vite-plugin-ssr
cd vite-plugin-ssr/examples/react-apollo/
npm run install
npm run start
```
