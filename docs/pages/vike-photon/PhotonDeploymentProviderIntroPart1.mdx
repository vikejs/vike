import { Link } from '@brillout/docpress'

We recommend using <Link href="/blog/photon">Photon</Link> for a seamless and full-fledged <a href={props.link}>{props.provider}</a> integration.

The entire documentation for using Photon with {props.provider} is contained on this page and the <Link href="/vike-photon"/> page.
