import { Link } from '@brillout/docpress'
import { PhotonBuiltInServer, PhotonBetaWarning, PhotonAutoLoad, PhotonDeploymentProviderIntroPart1, PhotonDeploymentProviderIntroPart2 } from '../vike-photon/components'

> Alternatively, for more control, you can <Link href="#manual-integration">manually integrate {props.provider}</Link> instead of using Photon.

<PhotonBetaWarning />

You can use {props.provider} to deploy <Link href="/pre-rendering">static websites</Link> as well as <Link href="/ssr">SSR</Link>.

> If you <Link href="/pre-rendering#partial">pre-render your pages only partially</Link>, you can statically deploy your pre-rendered pages while dynamically serving your SSR pages.

