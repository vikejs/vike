import { Link } from '@brillout/docpress'

Photon includes a built-in server that supports basic features like SSR. If you need additional server functionality (e.g. [file uploads](https://hono.dev/examples/file-upload) or <Link href="/api-routes">API routes</Link>), then create your own server:
