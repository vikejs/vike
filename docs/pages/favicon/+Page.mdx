import { Link, Warning } from '@brillout/docpress'
import { ProvidedBy, GlobalNote } from '../../components'

Type: `string | null | ((pageContext) => string | null | undefined)`  
Environment: server.  
Global: `true`.  

<ProvidedBy noCustomGuide />

<Warning>
  This setting is experimental and may be removed in the future. Consider <Link href="/Head">using `+Head`</Link> instead.
</Warning>

The `favicon` setting sets the page's favicon.

```js
// pages/+config.js

import favicon from './favicon.svg'

export default {
  favicon
}
```


## Global

<GlobalNote />


## See also

- <Link href="/head-tags" />
- <Link href="/settings#html-shell" doNotInferSectionTitle />
