import { Link, Warning } from '@brillout/docpress'

<Warning>
  `*.page*.js` files are deprecated in favor of `+*.js` files, see
  <ul>
    <li><Link href="/Config" /></li>
    <li><Link href="/Page" /></li>
    <li><Link href="/migration/v1-design" /></li>
  </ul>
</Warning>

To define an HTML-only page, we `export { Page }` in `.page.server.js` instead of `.page.js`. See:
 - <Link href="/Page" />.
 - <Link href="/render-modes#html-only" />.

> This also works for SFC components, e.g. `.page.server.vue`.
