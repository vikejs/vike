import { Integration } from '../components/Integration'

`vite-plugin-ssr` is being used with all kinds off CSS frameworks; we provide examples only for CSS framework integrations that are non-trivial.

<Integration toolTypeName="CSS framework" />
