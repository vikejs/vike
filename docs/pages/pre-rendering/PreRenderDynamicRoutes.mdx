> Instead of providing a list of URLs, if you want the route to be resolved dynamically on the client-side, then see the workaround described at [#1476 - Pre-rendered dynamic routes (static host deployment)](https://github.com/vikejs/vike/issues/1476).
