import { Link } from '@brillout/docpress'
import { ConfigSpec, ProvidedBy } from '../../components'

<ConfigSpec
  env="server"
  providedBy={<ProvidedBy extension="vike-server">the <code>+server</code> setting</ProvidedBy> }
>
```ts ts-only
import type { BuildOptions } from 'esbuild'
type Server =
  | string
  | {
      entry: string | { index: string; [name: string]: string }
      standalone?: boolean | { esbuild: BuildOptions }
      hmr?: boolean | 'prefer-restart'
    }
```
</ConfigSpec>

The documentation for the various `+server` settings lives at <Link href="/vike-server"></Link>.
