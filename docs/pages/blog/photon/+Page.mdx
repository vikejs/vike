import { Link } from '@brillout/docpress'
import { PhotonRationale } from '../../../components'

TODO

## What is Photon?

## Why Photon?

As we devloped `vike-server`, we realized most of the code is agnostic to Vike. It became clear that we can (and should!) decouple the code from Vike with a minmal amount of glue code.

We didn't plan to write an framewor
When we started working on `vike-server` we just wanted to simple a simple problem for our users.

<PhotonRationale isBlogPost/>

## See also

- <Link href="/vike-photon" />
- <Link href="/blog/photon" />
