import { Link } from 'libframe-docs/components'

NextAuth.js can be used with Vite + `vite-plugin-ssr`.

## Example

 - [github.com/s-kris/vite-ssr-next-auth](https://github.com/s-kris/vite-ssr-next-auth)

## `process.env.NEXTAUTH_*`

To access environment variables on the client-side, such as `process.env.NEXTAUTH_URL`, we can use
[`vite.config.js#define`](https://vitejs.dev/config/#define)
or
[`vite.config.js#envPrefix`](https://vitejs.dev/config/#envprefix).

> Do not add `NEXTAUTH_` to `envPrefix` as it would leak `NEXTAUTH_SECRET`.

See also:
- [Vite > Config > Environment Variables](https://vitejs.dev/config/#environment-variables)

> You can use `process.env` as usual for server (e.g. Express.js) code. (Since server code is not processed by Vite.)


## Fetch polyfill for `getSession()`

In order to use NextAuth.js's [`getSession()`](https://next-auth.js.org/getting-started/client#getsession) on the server-side, you'll need to globally install a fetch polyfill.

> If you use `getSession()` only on the client-side, then you don't need this.

> [Node v18 has native `fetch()` support](https://nodejs.org/en/blog/announcements/v18-release-announce/#fetch-experimental) and a polyfill may not be needed anymore. (To be tested.)

We can for example use [`node-fetch` to patch the global object](https://github.com/node-fetch/node-fetch#providing-global-access):

```js
// server.js

// Only install the polyfill on the server (e.g. Express.js). Browsers already implement
// `fetch()` natively and it would be wasteful to load the polyfill on the browser-side.

// Or some other `fetch()` polyfill, e.g. `cross-fetch`.
import fetch, { Headers, Request, Response } from 'node-fetch'

if (!globalThis.fetch) {
  globalThis.fetch = fetch
  globalThis.Headers = Headers
  globalThis.Request = Request
  globalThis.Response = Response
}
```

## See Also

- <Link href="/auth" />
