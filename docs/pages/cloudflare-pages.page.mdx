import { StaticHostDocIntro, StaticHostDocOutro } from './static-hosts.components.tsx'

<StaticHostDocIntro
  staticHostLink={<>, such as <a href="https://pages.cloudflare.com/">Cloudflare Pages</a></>}
/>

If we want Cloudflare Pages to build our app for us, `vite-plugin-ssr` requires Node.js `>=12.19.0` and we may need to <a href="https://developers.cloudflare.com/pages/platform/build-configuration#language-support-and-tools">Cloudflare Pages' default Node.js version</a>.

<StaticHostDocOutro />
