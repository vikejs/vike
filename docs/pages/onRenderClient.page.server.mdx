import { HookTypeScriptHints } from '../components/HookTypeScriptHints'
import { Link } from '@brillout/docpress'

The `render()` hook of `.page.client.js` has been renamed to `onRenderClient()` for the V1 design.

> It's just a rename: the hook is equivalent and its documentation is the same as <Link href="/render-client" />.
>
> The documentation will be moved to this page after the stable release of the V1 design.

See <Link href="/migration/v1-design#renamed-hooks" />.

{ /*NOTE(aurelien) move the content of render-client.page.server.mdx here*/ }

## TypeScript

```ts
export { onRenderClient }

import type { OnRenderClientAsync } from 'vike/types'
import { hydrateDom } from 'some-ui-framework'

const onRenderClient: OnRenderClientAsync = async (
  pageContext
): ReturnType<OnRenderClientAsync> => {
  const { Page } = pageContext
  await hydrateDom(Page) // SSR
}
```
