import { Integration } from '../components/Integration'

`vite-plugin-ssr` is being used with all kinds of CSS frameworks; we provide guides only when integration isn't straightforward.

<Integration toolTypeName="CSS framework" />

Examples not listed on the left-side navigation:
 - [Vite-plugin-ssr with Panda CSS](https://github.com/YannBirba/vps-panda-css)
