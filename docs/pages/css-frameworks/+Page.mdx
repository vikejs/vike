import { Example } from '../../components'
import { Integration } from '../../components/Integration'

Vike is being used with all kinds of CSS frameworks; we provide guides only when integration isn't straightforward.

<Integration toolTypeName="CSS framework" />

Examples not listed on the left-side navigation:
 - <Example timestamp="2023.08" repo="YannBirba/vps-panda-css" />
   > vite-plugin-ssr was the [previous name of Vike](https://vite-plugin-ssr.com/vike).
