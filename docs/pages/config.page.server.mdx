import { Link } from '@brillout/docpress'

`+config.js` defines the interface between your app and vite-plugin-ssr.

- How `+config.js` works: <Link href="/architecture#config-js" />
  - Also explains `/renderer/+config.js`
  - Also explains files such as `+Page.js`, `+route.js`, `+onBeforeRender.js`, etc.
- Rules for import statements inside `+config.js`: <Link href="/config-code-splitting#import-rules" />
- Config list: <Link href="/Config" />
