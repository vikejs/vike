import './DxContent.css'
import { Link } from '@brillout/docpress'
import { ExtraWidth } from '../../../../components'

export const DX_TwoColumnLayout = ({children, cut = 0.5}) => <div style={{ display: 'grid', gridTemplateColumns: `${cut*100}% 1fr`, gap: '1rem' }}>{children}</div>
export const DX_Small = ({ children, style }) => <span style={{ fontSize: '0.84em', color: '#aaa', ...style }}>{children}</span>
export const DX_Center = ({ children }) => <div style={{ display: 'flex', justifyContent: 'center' }}>{children}</div>
export const DX_H3 = ({ children, style }) => <div style={{textAlign: 'center', fontSize: '1.8em', fontWeight: 600, margin: 20, marginTop: 70, ...style}}>{children}</div>
export const DX_H4 = ({ children, style }) => <div style={{textAlign: 'center', fontSize: '1.3em', fontWeight: 500, margin: 15, ...style}}>{children}</div>
export const DX_FeatureWall = ({ children }) => <ExtraWidth><DX_Center><div className="feature-wall">{children}</div></DX_Center></ExtraWidth>
export const DX_Feature = ({ children, advanced }) => <div className={advanced && 'feature-advanced'}>{children}</div>
export const DX_Hr = () => <hr style={{background: '#aaa', height: 1, margin: 20}}/>
export const DX_ClosingWord = ({ children }) => <DX_Center><div style={{fontSize: '2em'}}>{children}</div></DX_Center>


<DX_TwoColumnLayout>
<div>

<DX_H3 style={{marginBottom: 22, marginTop: 22}}>
First-class SSR/SPA/SSG support
</DX_H3>

<div style={{fontSize: '1.4em'}}>
Toggle <Link href="/ssr">SSR</Link>/<Link href="/SSR-vs-SPA">SPA</Link>/<Link href="/pre-rendering">SSG</Link> on a page-by-page basis, powered by <Link href="/config#inheritance">Vike's config inheritance</Link>.
</div>

```yaml
# SSR pages // [!code highlight]
/pages/product/@id/+Page.ts         # URL: /product/42
/pages/product/@id/buy/+Page.ts     # URL: /product/42/buy

# SPA pages (i.e. SSR disabled) // [!code highlight]
/pages/admin/products/+Page.ts      # URL: /admin/products
/pages/admin/users/+Page.ts         # URL: /admin/users

# SSG pages (i.e. pre-rendering) // [!code highlight]
/pages/(marketing)/index/+Page.ts   # URL: /
/pages/(marketing)/about/+Page.ts   # URL: /about
```

</div>
<div>

```ts
// /pages/product/+config.js
// Config for product pages: /pages/product/**/+Page.ts // [!code highlight]
export default {
  ssr: true // [!code highlight]
}
```
```js
// /pages/admin/+config.js
// Config for admin pages: /pages/admin/**/+Page.ts // [!code highlight]
export default {
  ssr: false // SPA // [!code highlight]
}
```
```js
// /pages/(marketing)/+config.js
// Config for marketing pages: /pages/(marketing)/**/+Page.ts // [!code highlight]
export default {
  prerender: true // SSG // [!code highlight]
}
```
</div>

</DX_TwoColumnLayout>

<DX_H3>API</DX_H3>

<DX_H4>Next-gen RPC (aka Server Functions)...</DX_H4>

...via the next-generation tool [Telefunc](https://telefunc.com) (maintained by the Vike team), or use classic tools such as tRPC.

<DX_TwoColumnLayout>
<div>
```ts
// CreateTodo.telefunc.ts
// Environment: server

// Telefunc makes onNewTodo() remotely callable
// from the browser.
export { onNewTodo }

import { getContext } from 'telefunc'

// Telefunction arguments are automatically validated
// at runtime, so `text` is guaranteed to be a string.
async function onNewTodo(text: string) {
  const { user } = getContext()

  // With an ORM
  await Todo.create({ text, authorId: user.id })

  // With SQL
  await sql(
    'INSERT INTO todo_items VALUES (:text, :authorId)',
    { text, authorId: user.id }
  )
}
```
</div>
<div>
```ts
// CreateTodo.tsx
// Environment: client

// CreateTodo.telefunc.ts isn't actually loaded;
// Telefunc transforms it into a thin HTTP client.
import { onNewTodo } from './CreateTodo.telefunc.ts'

async function onClick(form) {
  const text = form.input.value
  // Behind the scenes, Telefunc makes an HTTP request
  // to the server.
  await onNewTodo(text)
}

function CreateTodo() {
  return (
    <form>
      <input input="text"></input>
      <button onClick={onClick}>Add To-Do</button>
    </form>
  )
}
```
</div>
</DX_TwoColumnLayout>

<DX_Hr/>

<DX_TwoColumnLayout cut={0.46}>
<div>
<DX_H4>First-class REST</DX_H4>
Consume REST APIs via Vike's built-in hook <Link href="/data">`+data`</Link>, or via extensions such as [`vike-react-query`](https://github.com/vikejs/vike-react/tree/main/packages/vike-react-query#readme)/[`vike-vue-query`](https://github.com/vikejs/vike-vue/tree/main/packages/vike-vue-query#readme)/[`vike-solid-query`](https://github.com/vikejs/vike-solid/tree/main/packages/vike-solid-query#readme).
</div>
<div>
<DX_H4>First-class GraphQL</DX_H4>
Consume GraphQL APIs via exensions such as [`vike-react-apollo`](https://github.com/vikejs/vike-react/tree/main/packages/vike-react-apollo#readme), or via <Link href="/graphql">custom integrations</Link>.
</div>
</DX_TwoColumnLayout>

<DX_TwoColumnLayout cut={0.46}>
<div>
```jsx
// Powered by vike-react-query

import { useSuspenseQuery } from '@tanstack/react-query'

const Movie = ({ id }) => {
  const result = useSuspenseQuery({
    queryKey: ['movie', id],
    queryFn: () =>
      fetch(`https://swapi.info/api/films/${id}`)
      .then((res) => res.json())
  })

  const { title } = result.data

  return (
    <div>
      Title: <b>{title}</b>
    </div>
  )
}
```
</div>
<div>
```jsx
// Powered by vike-react-apollo

import { useSuspenseQuery, gql } from '@apollo/client/index.js'

const Countries = () => {
  const { data } = useSuspenseQuery(gql`
    {
      countries {
        code
        name
      }
    }
  `)

  return (
    <ul>
      {data.countries.map((country) => (
        <li key={country.code}>{country.name}</li>
      ))}
    </ul>
  )
}
```
</div>
</DX_TwoColumnLayout>


<DX_H3>Powerful hooks</DX_H3>

<DX_TwoColumnLayout cut={0.55}>
<div>
<DX_H4>Data fetching: where</DX_H4>
Hooks give you control *where* data is fetched:
```js
// [!code word:server.]
// /pages/movie/@id/+data.server.js
// Environment: server // [!code highlight]

export async function data(pageContext) {
  const { id } = pageContext.routeParams
  // Fetching data on the server-side
  const movie = await Movie.find({ where: { id } })
  return { movie }
}
```
```js
// [!code word:client.]
// /pages/movie/@id/+data.client.js
// Environment: client // [!code highlight]

export async function data(pageContext) {
  const { id } = pageContext.routeParams
  // Fetching data on the client-side
  const response = await fetch(`https://swapi.info/api/films/${id}`)
  const movie = await response.json()
  return { movie }
}
```
</div>
<div>
<DX_H4>Data fetching: when</DX_H4>
Also control *when* data is fetched. When server starts:
```js
// [!code word:server.]
// pages/+onCreateGlobalContext.server.js
// Environment: server

export async function onCreateGlobalContext() {
  const someServerData = await fetchSomData()
  globalContext.someServerData = someServerData
}
```
When client-side navigation starts:
```js
// [!code word:client.]
// pages/+onCreateGlobalContext.client.js
// Environment: client

export async function onCreateGlobalContext() {
  const someClientData = await fetchSomData()
  globalContext.someClientData = someClientData
}
```
<Link href="/globalContext">`globalContext`</Link> can be accessed anywhere:
```js
import { getGlobalContext } from 'vike'
const { someGlobalData } = await getGlobalContext()
```
</div>
</DX_TwoColumnLayout>

<DX_TwoColumnLayout>
<div>
<DX_H4>Authorization</DX_H4>
```js
// /pages/admin/+guard.js
// Applies to all admin pages: /pages/admin/**/+Page.js

import { render } from 'vike/abort'

export async function guard(pageContext) {
  if (!pageContext.user.isAdmin) {
    throw render(401, "Access forbidden")
  }
}
```
</div>
<div>
<DX_H4>Instrumentation</DX_H4>
```js
// pages/+onHookCall.server.js
// Environment: server

// Called whenever a Vike hook is executed.

export async function onHookCall(hook, pageContext) {
  console.log('before', hook.name, hook.filePath)
  await hook.call()
  console.log('after', hook.name, hook.filePath)
}
```
</div>
</DX_TwoColumnLayout>

<DX_TwoColumnLayout cut={0.53}>
<div>
<DX_H4>Server integration</DX_H4>
Vike is just a middleware you can embed into any server.
```js
import { renderPage } from 'vike/server'

// Any server: Hono, Express.js, Cloudflare Worker, ...
server.addMiddleware({
  method: 'GET',
  route: '*', // catch-all
  async handler(request) {
    const pageContextInit = { urlOriginal: request.url }
    const pageContext = await renderPage(pageContextInit)
    const { body, statusCode, headers } = pageContext.httpResponse
    const response = { body, statusCode, headers }
    return response
  }
})
```
</div>
<div>
<DX_H4>i18n</DX_H4>
Hooks give you full control over i18n integration.
```js
// pages/+onBeforeRoute.js

export function onBeforeRoute(pageContext) {
  console.log(pageContext.urlOriginal) // /de-DE/filme
  const ret = extractLocale(pageContext.urlOriginal)
  const { urlWithoutLocale, locale } = ret
  console.log(locale) // de-DE
  console.log(urlWithoutLocale) // /movie
  // Make locale available as pageContext.locale
  pageContext.locale = locale
  // Vike's router will use pageContext.urlLogical
  // instead of pageContext.urlOriginal
  pageContext.urlLogical = urlWithoutLocale
}
```
</div>
</DX_TwoColumnLayout>


<DX_ClosingWord>
And a lot <Link href="/hooks">more hooks</Link>.
</DX_ClosingWord>

<DX_H3>Powerful extensions</DX_H3>

<Link href="/extensions">Vike extensions</Link> powered by the same hooks you use, for deep and seamless integrations.

<DX_TwoColumnLayout>
<div>
<DX_H4>UI framework extensions</DX_H4>
Use Vike with any UI framework — via Vike extensions for a quick start, or via <Link href="/integration#ui-framework">manual integration</Link> for maximum control and flexibility.
- vike-react / vike-vue / vike-solid
</div>
<div>
<DX_H4>Advanced UI framework integration</DX_H4>
Also advanced integrations with RSC (React Server Components).
- vike-react-rsc
</div>
</DX_TwoColumnLayout>

<DX_H4>Deep tool integrations</DX_H4>

Deep UI framework integrations...
<DX_TwoColumnLayout>
<div>
- vike-react-sentry
</div>
<div>
- vike-react-zustand
</div>
</DX_TwoColumnLayout>

<DX_ClosingWord>Growing extension ecosystem</DX_ClosingWord>


<DX_H3>Full-fledged</DX_H3>

<DX_FeatureWall>

<DX_Feature advanced>
Data fetching
<DX_Small>
Fetch data where you (client or server, or both) and when you want (server start, browser start, page render).
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
HTML streaming
<DX_Small>
- Progressive rendering (e.g. via [`react-streaming`](https://github.com/brillout/react-streaming)), Web Stream + Node.js Stream
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Client-side Routing
<DX_Small>
- Scroll restoration, <Link href="/navigate#history-pushstate">doesn't break `history.pushState()`</Link>, [supports URL text fragments](https://github.com/vikejs/vike/issues/2114).
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Config inheritance
<DX_Small>
<Link href="/config#inheritance">Apply configs to a single/group/all pages</Link>, <Link href="/meta">create your own configs</Link> and [build your own framework](https://land.vike.dev/#framework-as-a-product).
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Pre-rendering (aka SSG)
<DX_Small>
<Link href="/prerender#redirects">Redirects</Link>, <Link href="/data#client-js">client-side data fetching</Link>, partial pre-rendering, ISR.
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Server-Side Rendering (SSR)
<DX_Small>
First-class SSR integration with tools via powerful <Link href="/extensions">extensions</Link>/<Link href="/hooks">hooks</Link>.
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Redirections
<DX_Small>
<Link href="/redirect">Programmatic redirections</Link>, <Link href="/redirects">static reirections</Link>, <Link href="/render">programmatic URL rewrites</Link>, <Link href="/auth#login-flow">novel authentication flow</Link>.
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Routing
<DX_Small>
Programmatically <Link href="/route-function">define</Link> and <Link href="/guard">protect</Link> routes, Filesystem routing with <Link href="/file-structure">domain-driven file structure</Link>.
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Base URL
<DX_Small>
Base URL for static assets (CDN deployments) + (different) Base URL for SSR server.
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Internationalization (i18n)
<DX_Small>
Low-level hook +onBeforeRoute for full control over i18n
</DX_Small>
</DX_Feature>

<DX_Feature advanced>
Instrumentation
<DX_Small>
Hook measurement, slow hook warnings, multi-level error-tracking, ...
</DX_Small>
</DX_Feature>

<DX_Feature>
Layouts
<DX_Small>
<Link href="/Layout">Layouts</Link> and <Link href="/Layout#nested">nested layouts</Link>.
</DX_Small>
</DX_Feature>

<DX_Feature>
Link prefetching
<DX_Small>
<Link href="/prefetchStaticAssets">Links are prefetched</Link> upon mouse hover (or optionally upon entering the viewport).
</DX_Small>
</DX_Feature>

<DX_Feature>
Asset preloading
<DX_Small>
<Link href="/preloading">Assets are preloaded</Link> for quicker pages loads.
</DX_Small>
</DX_Feature>

<DX_Feature>
CSP
<DX_Small>
PCI DSS compliance
</DX_Small>
</DX_Feature>

<DX_Feature>
Any JavaScript runtime
<DX_Small>
Works with any JavaScript runtime (Node.js, Bun, Deno, Workers, Electron, Tauri, [Electrobun](http://blackboard.sh/electrobun), ...)
</DX_Small>
</DX_Feature>

<DX_Feature>
Rich context objects
<DX_Small style={{fontSize: '0.82em'}}>
  <Link href="/globalContext">`globalContext.{pages,config,...}`</Link>  
  <Link href="/pageContext">`pageContext.{isHistoryNavigation,...}`</Link>
</DX_Small>
</DX_Feature>

<DX_Feature>
Fault tolerant
<DX_Small>
E.g. Vike's runtime can be loaded twice (needed for some deployment strategies).
</DX_Small>
</DX_Feature>

<DX_Feature>
Infinite loop protections
<DX_Small>
Infinite URL redirects/rewrites are catched, preventing self-DDoS.
</DX_Small>
</DX_Feature>

<DX_Feature>
Conservative browser support
<DX_Small>
Supports [`@vitejs/plugin-legacy`](https://github.com/vitejs/vite/tree/main/packages/plugin-legacy) for legacy browsers.
</DX_Small>
</DX_Feature>

<DX_Feature>
Synced deploys
<DX_Small>
Frontend [automatically synced](https://vike.dev/deploy-sync) with SSR server.
</DX_Small>
</DX_Feature>

<DX_Feature>
Helpful warnings & errors
<DX_Small>
≥400 warnings/errors guiding DX/performance/security, component stack injected in stack trace.
</DX_Small>
</DX_Feature>

<DX_Feature>
Hackable
<DX_Small>
Access Vike internals via <Link href="/warning/internals">`pageContext.dangerouslyUseInternals`</Link>.
</DX_Small>
</DX_Feature>

<DX_Feature>
Included in Vite CI
<DX_Small>
[Vike is part of Vite's CI](https://github.com/vitejs/vite-ecosystem-ci/blob/main/tests/vike.ts)
</DX_Small>
</DX_Feature>

</DX_FeatureWall>
