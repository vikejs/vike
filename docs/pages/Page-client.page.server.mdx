import { Link } from 'vikepress'

To define an SPA page, we `export { Page }` in `.page.client.js` instead of `.page.js`. See:
 - <Link href="/Page" />.
 - <Link href="/render-modes#spa" />.

> This also works for SFC components, e.g. `.page.client.vue`.
