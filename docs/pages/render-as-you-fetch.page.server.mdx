import { Invitation } from '../components'

With React 18, we can stream the page's HTML while the page's data is being fetched.

This means that we can already show the static parts of the page's HTML to the user, before even starting to fetch the page's data.
As the page's data is being fetched, we further stream the page's HTML.

We are experimenting this new technique; <Invitation/> if you are interested in that space.
