import { Link } from '@brillout/docpress'
import { ImplementedBy } from '../../components'

Environment: client.
<ImplementedBy by={<Link href="/vike-react"><code>vike-react</code></Link>} noCustomGuide={true}>`onBeforeRenderClient()`</ImplementedBy>

Hook called before `onRenderClient()`.

It's usually used for integrating tools, such as state management tools.

## See also

- <Link href="/vike-react"/>
- <Link href="/onAfterRenderClient"/>
