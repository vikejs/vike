import { Link, Warning } from '@brillout/docpress'

You use `extends` to install <Link href="/vike-packages" noBreadcrumb />.

```js
// /pages/+config.h.js

import vikeReact from 'vike-react'

export default {
  // Inherit the configuration set by vike-react
  extends: vikeReact
}
```
