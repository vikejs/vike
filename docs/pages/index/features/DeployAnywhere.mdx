import { Emoji } from '@brillout/docpress'
import { PrerenderPreview } from './PrerenderPreview'
import { JustAMiddleware } from '../../../components'

### 単なるミドルウェア

<JustAMiddleware />

### プリレンダリング (aka SSG)

アプリをプリレンダリングして本番のNode.jsサーバーを不要にし、任意の静的ホスト（Cloudflare Pages、GitHub Pages、Netlifyなど）にデプロイすることもできます。

<PrerenderPreview />

<h3>
  Cloudflare Workers + Vike = <Emoji name="red-heart" />
</h3>

Vikeの無駄のないアーキテクチャのおかげで、あなたのアプリはCloudflare Workersの1MBバンドルサイズ制限に制限されることが少なくなります。
