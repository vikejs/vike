import { Link } from '@brillout/docpress'
import { UiFrameworkExtension } from '../../components'

With Vike, you have the choice between:
- <Link href="/extensions">Vike extensions</Link>, for a quick start.
- Custom integrations, for full control over how tools are integrated.

Use Vike extensions whenever possible — fall back to custom integrations only if you have a clear reason.

> Vike extensions are highly polished integrations. But they're also slightly opinionated and may not fit what you need.

For example:
1. Start using Vike with <UiFrameworkExtension name />.
1. If you run into an issue/blocker with <UiFrameworkExtension succinct />, start a discussion with the maintainers of <UiFrameworkExtension succinct /> on GitHub.
1. If a maintainer of <UiFrameworkExtension succinct /> replies that resolving your issue/blocker is out-of-scope, start considering a custom integration.

> You can also <Link href="/eject">eject Vike extensions</Link> to customize them.

## See also

- <Link href="/react#custom-integration" />
- <Link href="/vue#custom-integration" />
- <Link href="/solid#custom-integration" />
