import { Link } from '@brillout/docpress'
import { ImplementedBy } from '../../components'

Environment: client.
<ImplementedBy by={<Link href="/vike-react"><code>vike-react</code></Link>} noCustomGuide={true}>`onAfterRenderClient()`</ImplementedBy>

Same as <Link href="/onBeforeRenderClient">`onBeforeRenderClient()`</Link> but called at the end. It's usually used for integrating tools.

## See also

- <Link href="/onBeforeRenderClient"/>
- <Link href="/onRenderClient"/>
- <Link href="/onHydrationEnd" />
- <Link href="/hooks"/>
