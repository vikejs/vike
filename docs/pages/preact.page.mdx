`vite-plugin-ssr` is view framework agnostic and many users are using [Preact](https://preactjs.com/).

To get started, we can scaffold a Preact app with `$ npm init vite-plugin-ssr@latest` and use the `preact` or `preact-ts` boilerplates.

