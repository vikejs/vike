import { Link, Warning } from '@brillout/docpress'
import { UiFrameworkVikeExtensionNames } from '../../components'

We recommend using [Bati](https://batijs.dev) to scaffold a new Vike app.

> Apps scaffolded by Bati use <UiFrameworkVikeExtensionNames links/>.

## Without Vike Extension

You can also scaffold a new Vike app that doesn't use <UiFrameworkVikeExtensionNames links/>, which means you control the UI framework (React/Vue/Solid/...) integration.

> See <Link href="/why#optional-control" />.

<Warning>
We recommend using the following command only if:
 - you are an expert user and you want control over integration code, or
 - you are curious.
</Warning>

With [npm](https://www.npmjs.com):

```shell
npm init vike@latest
```

With [pnpm](https://pnpm.io):

```shell
pnpm create vike
```

With [Yarn](https://yarnpkg.com):

```shell
yarn create vike
```

Options:
- `--skip-git`: don't initialize a new Git repository

A prompt will let you choose between:
- `react`: React + JavaScript
- `react-ts`: React + TypeScript
- `vue`: Vue + JavaScript
- `vue-ts`: Vue + TypeScript

See also:
- [GitHub > `vikejs/vike` > `boilerplates/`](https://github.com/vikejs/vike/tree/main/boilerplates)
- [GitHub > `vikejs/vike` > `examples/react-minimal`](https://github.com/vikejs/vike/tree/main/examples/react-minimal)
- [GitHub > `vikejs/vike` > `examples/react-full`](https://github.com/vikejs/vike/tree/main/examples/react-full)
- [GitHub > `vikejs/vike` > `examples/vue-minimal`](https://github.com/vikejs/vike/tree/main/examples/react-minimal)
- [GitHub > `vikejs/vike` > `examples/vue-full`](https://github.com/vikejs/vike/tree/main/examples/vue-full)


## See also

- <Link href="/add" />
