import { Warning } from '@brillout/docpress'

To use vike with [Windi CSS](https://github.com/windicss/windicss):

1. Install:
   ```shell
   npm install vite-plugin-windicss windicss
   ```

2. Add `vite-plugin-windicss` to `vite.config.js`:
   ```js
   // vite.config.js

   import ssr from 'vike/plugin'
   import WindiCSS from 'vite-plugin-windicss'

   export default {
     plugins: [
       ssr(),
       WindiCSS({
         scan: {
           // By default only `src/` is scanned
           dirs: ["pages"],
           // We only have to specify the file extensions we actually use.
           fileExtensions: ["vue", "js", "ts", "jsx", "tsx"]
         }
       })
     ]
   }
   ```

   <Warning>Make sure that all your files are included in `scan.dirs`.</Warning>

   > These options can also be defined in `windi.config.js`.

3. Import `virtual:windi.css` in `_default.page.client.js`.
   ```js
   // renderer/_default.page.client.js

   import 'virtual:windi.css'

   // ...
   ```

## See also

 - [Windi CSS > Integration for Vite](https://windicss.org/integrations/vite.html)

## Example

Example of using Vike with Windi CSS:
 - [GitHub > `brillout/vike_windi-css`](https://github.com/vikejs/vike_windi-css)
