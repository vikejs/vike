import { Link } from '@brillout/docpress'

In principle, you can use Vike with any JavaScript server.

Vike is server agnostic and, from the perspective of a JavaScript server, Vike is just a server middleware:
 - <Link href="/integration#server-side-tools" />
