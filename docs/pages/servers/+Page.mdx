import { Link } from '@brillout/docpress'

Vike is server agnostic: we can use it within any kind of server environment since, from the perspective of a server architecture, Vike is just a server-agnostic middleware.

See:
 - <Link href="/integration#server-side-tools" />
