import { Link, Warning } from '@brillout/docpress'

<Warning>
  `*.page*.js` files are deprecated in favor of `+*.js` files, see
  <ul>
    <li><Link href="/Config" /></li>
    <li><Link href="/route" /></li>
    <li><Link href="/migration/v1-design" /></li>
  </ul>
</Warning>

Environment: server (and browser if using Client Routing).

The `_default.page.route.js` file allows us to control the routing process:
 - [`export { filesystemRoutingRoot }`](filesystemRoutingRoot)
 - [`export { onBeforeRoute }`](onBeforeRoute)
