import { Danger, Link } from '@brillout/docpress'

<Danger>Support for executing more than one `onBeforeRender()` hook is deprecated. Use <Link href="/meta" text="custom configs"/> instead. (You can still define multiple `onBeforeRender()` hooks but only one will be executed.)</Danger>
