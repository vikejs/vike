import { Link } from '@brillout/docpress'
import { Tab, Tabs, TabList, TabPanel } from 'react-tabs'
import { FreeTrialForm } from '@vikejs/license-components'
import '../../components/tabs.css'

export const freeForeverBody = `== URL

[Your app's URL (\`window.location.hostname\`)]

== Reason

[Why your organization has limited financial resources]`

export const extendedBody = `${freeForeverBody}

== Extend duration

Two years`


The free license program ensures everyone can use Vike.


## 3-month free trial

Get your key for a 3-month free trial for your domains:

<FreeTrialForm />


## Grants

If a license is too expensive for your organization, apply for a free grant — `Extended free trial` (free for a set period) or `Forever free` (free indefinitely).

### Who's eligible?

Your organization is eligible if it has limited financial resources and purchasing a license would be too expensive.

> In general, we consider an organization to have limited financial resources if it has **fewer than 10 employees**, though organizations in **lower-income countries can exceed this threshold**.
>
> You can still apply if you have more than 10 employees — financial constraints matter more than headcount. However, apply only if you genuinely cannot afford a license, not simply to reduce costs.

Apply for:
- `Extended free trial` if you expect more financial resources in the future
- `Forever free` otherwise

<p/>
> For example:
> - Startup => `Extended free trial`
> - Open Source (without much revenue) => `Forever free`
> - Amateur sports club => `Forever free`

You can also apply for `Extended free trial` for other reasons (e.g. pending clearance from your company's finance department).

We tend to be generous, so don't hesitate to apply — it's easy and quick.


### How to apply?

Contact us to apply.

<Tabs>
  <TabList>
    <Tab>`Extended free trial`</Tab>
    <Tab>`Forever free`</Tab>
  </TabList>
  <TabPanel>

<span>Email (pre-filled subject & body): </span>
<a
  href={
    "mailto:support@vike.dev" +
    "?subject=" + encodeURIComponent("Extended free trial") +
    "&body=" + encodeURIComponent(extendedBody)
  }
>
  {'support@vike.dev'}
</a>

Subject:
```
Extended free trial
```

Body:

```
== URL

[Your app's URL (`window.location.hostname`)]

== Reason

[Why your organization has limited financial resources]

== Extend duration

Two years. [Can be modified: for example, you can set it to the duration after which you
expect to easily afford a license. You can apply again after the extended period ends.]
```

  </TabPanel>
  <TabPanel>

<span>Email (pre-filled subject & body): </span>
<a
  href={
    "mailto:support@vike.dev" +
    "?subject=" + encodeURIComponent("Forever free") +
    "&body=" + encodeURIComponent(freeForeverBody)
  }
>
  {'support@vike.dev'}
</a>

Subject:
```
Forever free
```

Body:

```
== URL

[Your app's URL (`window.location.hostname`)]

== Reason

[Why your organization has limited financial resources]
```

  </TabPanel>
</Tabs>


## See also

- <Link href="/pricing" />
- <Link href="/license" />
