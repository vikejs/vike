import { Link } from '@brillout/docpress'

We can use VPS with any kind of server environment since, from a server perspective, VPS is just an agnostic middleware. See:
 - <Link href="/integration" />
 - <Link href="/integration#server-tools" />
