import { Link } from '@brillout/docpress'

Vite-plugin-ssr is server agnostic: we can use vite-plugin-ssr with any kind of server environment because, from a server perspective, vite-plugin-ssr is just an server agnostic middleware.

See:
 - <Link href="/integration#server-tools" />
