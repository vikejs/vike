import { Link } from '@brillout/docpress'
import { StaticHostDocIntro, StaticHostDocOutro, StaticHostDocStrategies } from '../../components/static-host'

> Cloudflare Pages is [being deprecated](https://blog.cloudflare.com/full-stack-development-on-cloudflare-workers/#start-with-workers) — you can still use it, but the official recommendation is to start new projects with [Cloudflare Workers](https://workers.cloudflare.com) instead.

For deploying static websites (aka <Link href="/pre-rendering#ssg">SSG / pre-rendered apps</Link>) — see <Link href="/cloudflare" />.

## See also

- <Link href="/cloudflare" />
