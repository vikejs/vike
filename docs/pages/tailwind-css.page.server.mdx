import { Link } from '@brillout/docpress'

See Tailwind's [Vite Installation Guide](https://tailwindcss.com/docs/guides/vite).

See <Link href="/windi-css" /> for using [Windi CSS](https://github.com/windicss/windicss) instead.

## Examples

- Minimal example using `vite-plugin-ssr` + Tailwind CSS: [GitHub > `brillout/vite-plugin-ssr_tailwind-css`](https://github.com/brillout/vite-plugin-ssr_tailwind-css).
- Example using `vite-plugin-ssr` + Storeon + `@phensley/cldr` + TailwindCSS + PostCSS + `schema-dts`: [GitHub > `mate-h/vite-ssr-web`](https://github.com/mate-h/vite-ssr-web)
