export const noHeading = true
export const pageTitle = 'SSR (Server Side Rendering)'

**Server-side Rendering (SSR)** denotes the practice of rendering the content of a page to HTML at request-time.

SSR is only one of many rendering modes, see [Guides > SPA, SSR, SSG, HTML-only](/render-modes) to explore all rendering modes.
