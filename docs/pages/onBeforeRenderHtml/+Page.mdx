import { Link } from '@brillout/docpress'
import { ProvidedBy, UiFrameworkExtension } from '../../components'

Environment: server  

<ProvidedBy list={['vike-react', 'vike-vue']} noCustomGuide={true}>`onBeforeRenderHtml()`</ProvidedBy>

Same as <Link href="/onAfterRenderHtml">`onAfterRenderHtml()`</Link> but called right before the `+Page` component is rendered to HTML.
