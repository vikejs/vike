import { Link } from '@brillout/docpress'
import { ProvidedBy, UiFrameworkExtension, ConfigSpec } from '../../components'

<ConfigSpec
  env="server"
  cumulative
  providedBy={<ProvidedBy list={['vike-react', 'vike-vue']} noCustomGuide={true}><code>onBeforeRenderHtml()</code></ProvidedBy>}
/>

Same as <Link href="/onAfterRenderHtml">`onAfterRenderHtml()`</Link> but called right before the `+Page` component is rendered to HTML.
