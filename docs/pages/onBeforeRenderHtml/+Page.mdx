import { Link } from '@brillout/docpress'
import { UiFrameworkExtension, ConfigSpec } from '../../components'

<ConfigSpec
  env="server"
  cumulative
  providedByExtension={{ kind: 'hook', name: 'onBeforeRenderHtml()', extension: ['vike-react', 'vike-vue'], noCustomGuide: true }}
/>

Same as <Link href="/onAfterRenderHtml">`onAfterRenderHtml()`</Link> but called right before the `+Page` component is rendered to HTML.
