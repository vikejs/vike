import { Link } from '@brillout/docpress'
import { ConfigSpec } from '../../components'

<ConfigSpec
  env="server"
  cumulative
  global
  providedByExtension={{ kind: 'setting', name: 'bodyHtmlBegin', extension: ['vike-vue', 'vike-react'], noCustomGuide: true }}
/>

Setting for inserting HTML at the beginning of the `<body>` tag, see the documentation at <Link href="/bodyHtmlEnd">`bodyHtmlEnd`</Link>.
