import { Link } from '@brillout/docpress'
import { ProvidedBy, UiFrameworkExtension, GlobalNote } from '../../components'

Environment: server.

<ProvidedBy list={['vike-vue', 'vike-react']} noCustomGuide>`bodyHtmlBegin`</ProvidedBy>

Setting for inserting HTML at the beginning of the `<body>` tag, see the documentation at <Link href="/bodyHtmlEnd">`bodyHtmlEnd`</Link>.
