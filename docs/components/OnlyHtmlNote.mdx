import { Link } from '@brillout/docpress'

It only generates `<head>` tags while rendering the HTML of the first page the user visits: the `<head>` tags aren't updated upon client-side page navigation. The reason is that it's only meant for HTML crawlers (most notably search engine bots), see explanation at <Link href="/Head#only-html" doNotInferSectionTitle />.
