import { Warning } from '@brillout/docpress'

<Warning>
  `express` and `fastify` are not compatible with Bun, Deno or Cloudflare targets, and thus are not recommended by the Vike team.

  Prefer a server with native `fetch` support, like Hono.
</Warning>