import { Warning } from '@brillout/docpress'

<Warning>
  Because {props.name} doesn't follow modern JavaScript standards (such as `fetch`), it isn't compatible with Bun, Deno, or Cloudflare.
</Warning>
