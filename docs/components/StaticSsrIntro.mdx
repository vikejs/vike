import { Link } from '@brillout/docpress'

You can use {props.provider} to deploy <Link href="/pre-rendering">static websites</Link> as well as <Link href="/ssr">SSR</Link>.

> If you <Link href="/pre-rendering#partial">pre-render partially</Link>, then your pre-rendered pages are statically deployed while your SSR pages are served dynamically.
