import { Link } from '@brillout/docpress'

{!props.isBlogPost && <p><b>Why Photon?</b></p>}

Vite has become a common build infratstrure shared between many frameworks, and that's a great. Everyone

We believe the same can (and eventually will) happen with the server and deployment architecture.

We believe Photon to represent the (inevitable) next step towards the future.

{!props.isBlogPost && <p>See <Link href="/blog/photon#why-photon" doNotInferSectionTitle /></p>}
