import { Link } from '@brillout/docpress'

If you didn't already, define <Link href="/server#get-started">`+server.js`</Link> with your favorite server (Hono, Express, ...), or <Link href="/server#server-true">set `+server` to `true`</Link> to use Vike's built-in lightweight server.
